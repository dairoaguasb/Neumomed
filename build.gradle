// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext.kotlin_version = '1.3.72'
    repositories {
        google()
        jcenter()
        
    }
    dependencies {
        classpath "com.android.tools.build:gradle:${CoreVersion.GRADLE}"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:${CoreVersion.KOTLIN}"
        classpath "android.arch.navigation:navigation-safe-args-gradle-plugin:${CoreVersion.SAFE_ARGS}"
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
